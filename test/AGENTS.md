## Available Functions

The `test-*.rkt` files contain webracket programs.
They can only use primitives from webracket.
The list of available primitives are in `compiler.rkt`.

There is no support for exceptions or exception handlers
in webracket, so do not write tests for failure handling.

