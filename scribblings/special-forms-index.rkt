#lang racket/base

;; This file is generated by scribblings/generate-special-forms-index.rkt.
;; To refresh it, run:
;;   racket scribblings/generate-special-forms-index.rkt

(provide documented-special-forms)

(define documented-special-forms
  '(
    if
    cond
    case
    and
    or
    when
    unless
    lambda
    λ
    case-lambda
    let
    let*
    letrec
    let-values
    let*-values
    letrec-values
    local
    define
    define-values
    begin
    begin0
    set!
    set!-values
    for
    for*
    for/list
    for*/list
    for/vector
    for*/vector
    for/sum
    for*/sum
    for/fold
    for*/fold
    for/or
    for*/or
    for/and
    for*/and
    for/first
    for*/first
    quasiquote
    unquote
    unquote-splicing
    with-handlers
    block
    require-lib
    include-lib
    #%app
    #%module-begin
    ))
